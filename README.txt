Optimisation du groupe shutdown/r

Fonctionnement du programme :

Il y a deux programmes, un en C#, notre premier programme et qui ne permet pas d'utiliser l'API mais génère les transactions en .JSON; et l'autre en Python, qui permet d'utiliser l'API